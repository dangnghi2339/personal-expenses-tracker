<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.login"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".LocaleApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Moneyme">

        <!-- LAUNCHER -->
        <activity
            android:name=".Onboarding01Activity"
            android:exported="true"
            android:theme="@style/Theme.Moneyme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".Onboarding02Activity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".IndividualActivity"/>
        <activity android:name=".MoneyActivity"/>
        <activity android:name=".NotificationActivity"/>
        <activity android:name=".LanguageActivity"/>

    </application>
</manifest>
