<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme mặc định của app -->
    <style name="Theme.Moneyme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- màu bạn đã định nghĩa -->
        <item name="colorPrimary">@color/blue_600</item>
        <item name="colorPrimaryContainer">@color/blue_400</item>
    </style>

    <!-- Theme riêng cho Splash/Onboarding01 -->
    <style name="Theme.Moneyme.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/blue_600</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/blue_600</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
